import serversActionTypes from './action_types';
import * as serversActions from './actions';
import serversReducer from './reducer';

export const actionTypes = serversActionTypes;
export const actions = serversActions;
export const reducer = serversReducer;
